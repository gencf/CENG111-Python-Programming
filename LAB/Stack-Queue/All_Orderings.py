from Stack import Stack

def all_orderings(l):
    st = Stack()
    a = []
    for i in l:
        if not st.is_empty():
            if len(st.top()) == len()
            a = st.pop()
        
        else:
            a = []

        if len()
        a += [i]



        
